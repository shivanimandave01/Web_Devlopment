let number = 10;
let age = "10";

//In JavaScript, `==` is the equality operator that checks for value equality, while `===` is the strict equality operator that checks for both value and type equality.

console.log(number == age);  // true (value equality)
console.log(number === age); // false (value and type equality)